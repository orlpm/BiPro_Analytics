@model BiPro_Analytics.Models.SeguimientoCovid
@{
    ViewData["Title"] = "Riesgos de contagio";
}

<input type="hidden" id="HdnIdEmpresa" value="@ViewBag.IdEmpresa" />

<section id="trabajadores" class="d-flex align-items-cente min-vh-100">
    <div class="container">

        <div class="section-title">
            <h2>Seguimiento de covid</h2>
        </div>

        <a href="#graficos" class="btn btn-info active scrollto" role="button" aria-pressed="true">Gráficos y estadisticas</a>
        <hr />

        <form asp-action="Index" method="get">
            <div class="form-actions no-color">
                <div class="form-row">
                    <div class="col-md-6 form-group">
                        <select asp-for="IdTrabajador" asp-items="@(new SelectList(@ViewBag.Trabajadores,"Id","Trabajador"))">
                            <option selected disabled>Seleccionar trabajador</option>
                        </select>
                    </div>
                    <div class="col-md-3 form-group">
                        <input type="submit" value="Buscar" class="btn btn-secondary" />
                    </div>
                </div>
            </div>
        </form>

        @*<form asp-action="Index" method="get">
            <div class="form-actions no-color">
                <div class="form-row">
                    <div class="col-md-3 form-group">
                        <select title="Unidad" name="IdUnidad" asp-items="@(new SelectList(@ViewBag.Unidades,"Id","Unidad"))">
                            <option selected disabled>Seleccionar unidad</option>
                        </select>
                    </div>
                    <div class="col-md-3 form-group">
                        <select name="IdArea" asp-items="@(new SelectList(@ViewBag.Areas,"Id","Area"))">
                            <option selected disabled>Seleccionar Area</option>
                        </select>
                    </div>
                    <div class="col-md-3 form-group">
                        <input type="submit" value="Buscar" class="btn btn-secondary" />
                    </div>
                </div>
            </div>
        </form>*@
        <div class="form-row">
            <div class="col-md-4 form-group">
                <a class="btn btn-secondary" asp-action="Index">Regresar lista completa</a>
            </div>
        </div>

        <div class="form-row">
            <div class="col-md-4 form-group">
                <a class="btn btn-secondary" asp-action="Create">Crear nuevo</a>
            </div>
        </div>

    </div>
</section>

<hr />

<section id="graficos">
    <input type="hidden" id="HdnIdEmpresa" value="@ViewBag.IdEmpresa" />

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6 col-sm-6  ">
                @*T4.A quienes se deben hacer pruebas para regresar a laborar. Asintomaticos con mas de 2 semanas.*@
                <form asp-action="ExportTrabajadoresSeguimientoGralCSV" method="get">
                    <div class="form-actions no-color">
                        <h3>Descargar seguimiento general de pacientes</h3>
                        <div class="form-row">
                            <div class="col-md-3 form-group">
                                @if (User.IsInRole("Admin"))
                                {
                                    <select name="IdEmpresa" class="form-control" asp-items="@(new SelectList(@ViewBag.Empresas,"Id","Empresa"))">
                                        <option selected disabled>Seleccionar Empresa</option>
                                    </select>
                                }
                                else
                                {
                                    <select name="IdEmpresa" class="form-control" asp-items="@(new SelectList(@ViewBag.Empresas,"Id","Empresa", @ViewBag.Empresas[0].Id))">
                                        <option selected disabled>Seleccionar Empresa</option>
                                    </select>
                                }
                            </div>
                            <div class="col-md-3 form-group">
                                <input type="submit" value="Descargar" class="btn btn-secondary" />
                            </div>
                        </div>
                    </div>
                </form>
                @*G4. A quienes se deben hacer pruebas de seguimiento*@
                <div class="x_panel">
                    <div class="x_title">
                        <h3>Seguimiento de síntomas</h3>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <canvas id="pieSeguimientoSintomas"></canvas>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-sm-6  ">
                @*G5. Seguimiento general de contagiados*@
                <div class="x_panel">
                    <div class="x_title">
                        <h3>Seguimiento general de contagiados</h3>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <canvas id="SeguimientoUbicacionesPie"></canvas>
                    </div>
                </div>
            </div>
        </div>

        <!--<div class="row">
            <div class="col-md-6 col-sm-6  ">-->
                @*M8. Pacientes que llenaron y no llenaron su F4*@
                <!--<form asp-action="PacientesNoLlenaronRC" method="get">
                    <div class="form-actions no-color">
                        <h3>Descargar pacientes que llenaron y no llenaron reporte contagio</h3>
                        <div class="form-row">
                            <div class="col-md-3 form-group">
                                <select asp-for="IdTrabajador" class="form-control" asp-items="@(new SelectList(@ViewBag.Empresas,"Id","Empresa"))">
                                    <option selected disabled>Seleccionar Empresa</option>
                                </select>
                            </div>
                            <div class="col-md-3 form-group">
                                <input type="submit" value="Buscar" class="btn btn-secondary" />
                            </div>
                        </div>
                    </div>
                </form>-->
                @*R8. Pacientes que no llenaron su F4*@
                <!--<div class="x_panel">
                    <div class="x_title">
                        <h3>Pruebas de seguimiento</h3>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <canvas id="GrGrR8NoLLenaronF4"></canvas>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-sm-6  ">-->
                @*M9. AM de seguimiento. Quienes llenaron el seguimiento y quienes no en todo el mes.*@
                <!--<form asp-action="QuienNoLlenaronSeguimientoCovidMen" method="get">
                    <div class="form-actions no-color">
                        <h3>Descargar quien lleno seguimiento y quien no en todo el mes</h3>
                        <div class="form-row">
                            <div class="col-md-3 form-group">
                                <select asp-for="IdTrabajador" class="form-control" asp-items="@(new SelectList(@ViewBag.Empresas,"Id","Empresa"))">
                                    <option selected disabled>Seleccionar Empresa</option>
                                </select>
                            </div>
                            <div class="col-md-3 form-group">
                                <input type="submit" value="Buscar" class="btn btn-secondary" />
                            </div>
                        </div>
                    </div>
                </form>-->
                @*R9. AM. Quienes no llenaron su F4 en todo el mes.*@
                <!--<div class="x_panel">
                    <div class="x_title">
                        <h3>Seguimiento general de contagiados</h3>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <canvas id="GrGrR9NoLLenaronF4Mens"></canvas>
                    </div>
                </div>
            </div>
        </div>-->
    </div>

</section>
