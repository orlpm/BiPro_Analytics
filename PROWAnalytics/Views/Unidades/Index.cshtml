@model IEnumerable<PROWAnalytics.Models.Unidad>

@{
    ViewData["Title"] = "Index";
}

<section id="adminRelaciones" class="d-flex  min-vh-100">
    <div class="container">
        <h1>Lista de unidades</h1>

        @if (ViewBag.Unidades != null)
        {
            <div class="alert alert-success">
                <strong>¡Vamos bien!</strong> Ya tienes unidades, puedes ingresar más.
            </div>
        }
        else
        {
            if (!ViewBag.SinUnidades)
            {
                <div class="alert alert-success">
                    <strong>¡Vamos bien!</strong> Para tener un mayor margen estadístico ingresa las unidades de tu empresa.
                </div>
                <div class="alert alert-info">
                    <strong>Nota!</strong> Puedes continuar sin especificar unidades
                    <a class="btn btn-warning" asp-action="IndicarSinUnidades">Continuar sin especificar</a>
                </div>
            }
            else
            {
                <div class="alert alert-success">
                    <strong>¡Ingresa unidades!</strong> Para tener un mayor margen estadistico ingresa las unidades de tu empresa.
                </div>
                <div class="alert alert-info">
                    <strong>Nota!</strong> Puedes habilitar las unidades
                    <a class="btn btn-info" asp-action="IndicarSinUnidades">Habilitar unidades</a>
                </div>
            }
        }

        @if (!ViewBag.SinUnidades)
        {
            <p>
                <a asp-action="Create">Ingresar nueva unidad</a>
            </p>
            <table id="tableTransparent" class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nombre)
                        </th>
                        @if (User.IsInRole("Admin"))
                        {
                            <th>
                                @Html.DisplayNameFor(model => model.IdEmpresa)
                            </th>
                        }
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nombre)
                            </td>
                            @if (User.IsInRole("Admin"))
                            {
                                <td>
                                    @Html.DisplayFor(modelItem => item.IdEmpresa)
                                </td>

                            }
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                                <a asp-action="Details" asp-route-id="@item.Id">Detalles</a> |
                                <a asp-action="Delete" asp-route-id="@item.Id">Borrar</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }

    </div>
</section>